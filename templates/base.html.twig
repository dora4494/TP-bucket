<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Bucket-List</title>
    <link rel="icon"
          href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Mynerve&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="{{ asset('home.css') }}">
    <link rel="icon" href="{{ asset('img/favicon.png') }}">


    {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
    {% block stylesheets %}
        {{ encore_entry_link_tags('app') }}

    {% endblock %}

    {% block javascripts %}
        {{ encore_entry_script_tags('app') }}
    {% endblock %}
</head>


<body>

<header>
    <div class="container-header">
        <div class="logo-and-title">
            <img src="{{ asset('img/logo.png') }}" alt="home" class="logo">
            <h1 class="Bucket-List"> Bucket-List</h1>
        </div>
        <nav>
        <a href="{{ path('main_about') }}" class="lien-about">About us</a>
            <a href="{{ path('wish_list') }}" class="lien-list">Liste</a>
            <a href="{{ path('app_register') }}" class="lien-list">S'inscrire</a>
            <a href="{{ path('app_login') }}" class="lien-list">Se connecter</a>
            <a href="{{ path('app_logout') }}" class="lien-list">Se déconnecter</a>
        </nav>
    </div>
</header>

<main>
    <div class="container-main">
        {% block body %}{% endblock %}



    </div>
</main>

<footer>
    <div class="container-footer">
        <div class="footer-text">&copy; Bucket-List - {{ "now" | date('Y') }}}</div>
        </div>
</footer>

</body>
</html>
